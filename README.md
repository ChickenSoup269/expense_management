# Deloy app

View app in:

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in UI web to use Gemini AI
3. Run the app:
   `npm run dev`
